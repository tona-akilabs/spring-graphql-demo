scalar Date

type Query {
    projects(page: Int!, size: Int!): ProjectPagination
    projectById(id: ID!): Project

    all: [Book]
    bookById(id: ID!): Book
}

type Mutation {
    addBook(book: BookInput!): Book
}